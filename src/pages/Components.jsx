export default function Components() {
  return (
    <div className="page">
      <h1>Components</h1>
      <p>A living library of UI components — buttons, cards, inputs, and more.</p>
    </div>
  )
}
